---
inclusion: always
priority: 1
description: "Approved technologies and dependencies. Changes require architecture review."---

# Technology Stack

## Core Technologies

### Backend
- **Language:** ## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.
 3.11|## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.
 18|## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.
 1.21|## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.

- **Framework:** {FastAPI|Express|Gin|## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.

- **Runtime:** {CPython|Node|## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.


### Frontend
- **Framework:** # Note: No Frontend section for CLI tools
```

```markdown 18|# Note: No Frontend section for CLI tools
```

```markdown 3|# Note: No Frontend section for CLI tools
```

```markdown|## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.

- **Language:** {TypeScript|JavaScript|## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.

- **Styling:** {Tailwind|Styled Components|## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.


### Database
- **Primary:** ### Database
- **Primary:** {Database if applicable}
 15|Mon## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.
DB 6|## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.

- **Cache:** # View cached LLM responses
cat .kiro/.cache/response_cache.json | jq
```
 7|## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.

- **ORM/ODM:** {SQLAlchemy|Prisma|Mon## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.
ose|## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.


### Infrastructure
- **Container:** {Docker|## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.

- **Orchestration:** {K8s|Docker Compose|## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.

- **Cloud:** {AWS|GCP|Azure|## v2.1.0 Shared Backend Architecture

The v2.1.0 release introduced a **Shared Backend Architecture** that unifies CLI and Power (MCP) implementations.


## Key Dependencies
| Purpose | Library | Version | Notes |
|---------|---------|---------|-------|
| Auth | ## Key Dependencies
| Purpose | Library | Version | Notes |
|---------|---------|---------|-------|
| CLI | {library} | {version} | {why} |
| Testing | {library} | {version} | {why} |
 | ## Key Dependencies
| Purpose | Library | Version | Notes |
|---------|---------|---------|-------|
| CLI | {library} | {version} | {why} |
| Testing | {library} | {version} | {why} |
 | ## Key Dependencies
| Purpose | Library | Version | Notes |
|---------|---------|---------|-------|
| CLI | {library} | {version} | {why} |
| Testing | {library} | {version} | {why} |
 |
| Testing | ## Key Dependencies
| Purpose | Library | Version | Notes |
|---------|---------|---------|-------|
| CLI | {library} | {version} | {why} |
| Testing | {library} | {version} | {why} |
 | ## Key Dependencies
| Purpose | Library | Version | Notes |
|---------|---------|---------|-------|
| CLI | {library} | {version} | {why} |
| Testing | {library} | {version} | {why} |
 | ## Key Dependencies
| Purpose | Library | Version | Notes |
|---------|---------|---------|-------|
| CLI | {library} | {version} | {why} |
| Testing | {library} | {version} | {why} |
 |

## Rationale
{Why this stack? Trade-offs considered?}